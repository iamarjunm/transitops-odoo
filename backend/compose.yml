version: "3.9"

services:
  backend:
    image: odoo-backend
    container_name: odoo-backend
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env